# ── Postgres ──────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=demo-db
DB_USER=postgres
DB_PASSWORD=culPepper12!

# ── JWT ───────────────────────────────────────────────────
# Generate a strong secret: python3 -c "import secrets; print(secrets.token_hex(48))"
JWT_SECRET=9b3ca92e44e700409e9a5bfd6d239318d793b03a64eba3bbc086e00f6eb5a40c740b7d9c79198d8032d5db99662deb80
JWT_EXPIRY_HOURS=8

# ── Server ────────────────────────────────────────────────
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# ── Security ──────────────────────────────────────────────
# Max failed logins before account lockout
LOGIN_MAX_ATTEMPTS=5
# Rate limit: max signin attempts per IP per minute
RATE_LIMIT_SIGNIN=8

# ── Email (SMTP) ───────────────────────────────────────────
# Leave SMTP_HOST empty to print emails to console (dev mode)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=geoffreysakora@gmail.com
SMTP_PASSWORD=wrra tlay phof uvhd
SMTP_FROM=geoffreysakora@gmail.com
APP_BASE_URL=http://localhost:3000

# ── CORS ──────────────────────────────────────────────────
ALLOWED_ORIGIN=http://localhost:3000